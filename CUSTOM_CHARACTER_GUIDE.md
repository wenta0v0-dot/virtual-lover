# ✨ 自定义角色创建功能 - 使用指南

## 🎉 功能介绍

现在你可以创建属于自己的**专属虚拟恋人**了！完全自定义角色的：

- 👤 **基本信息**：名称、性别、标题
- 🎨 **外貌形象**：上传头像、描述外貌
- 🧠 **人物设定**：性格、说话方式、行为特点
- 💬 **开场白**：角色对你说的话
- 🏷️ **个性标签**：温柔、高冷、活泼等

---

## 🚀 快速开始（3步搞定）

### 步骤1：创建数据库表 ⚠️ **必须执行**

在 **Neon 控制台** 执行SQL：

1. 访问 https://console.neon.tech
2. 选择你的数据库
3. 点击 **"SQL Editor"**
4. 复制 [create-custom-characters-table.sql](scripts/create-custom-characters-table.sql) 的内容并执行

或者直接执行这条简化版：

```sql
CREATE TABLE IF NOT EXISTS custom_characters (
    id SERIAL PRIMARY KEY,
    user_id INTEGER NOT NULL REFERENCES users(id),
    name VARCHAR(100) NOT NULL,
    title VARCHAR(100),
    tags JSONB DEFAULT '[]',
    avatar VARCHAR(10) DEFAULT '🎭',
    avatar_image VARCHAR(500),
    gender VARCHAR(10) DEFAULT 'female',
    appearance TEXT,
    system_prompt TEXT NOT NULL,
    greeting TEXT NOT NULL,
    color VARCHAR(7) DEFAULT '#FFB6C1',
    status VARCHAR(200),
    is_active BOOLEAN NOT NULL DEFAULT true,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 添加 is_custom 字段到 chat_sessions 表
ALTER TABLE chat_sessions ADD COLUMN IF NOT EXISTS is_custom BOOLEAN DEFAULT false;
```

### 步骤2：访问创建页面

1. 打开首页 http://localhost:3000
2. 点击 **"✨ 创建自定义角色"** 按钮
3. 或直接访问：http://localhost:3000/create-character

### 步骤3：填写角色信息

#### 📝 基本信息

- **角色名称**（必填）：给你的角色起个名字，如"小樱"、"阿明"
- **角色标题**：如"温柔学姐"、"神秘少年"
- **性别选择**：男性或女性角色
- **性格标签**：点击推荐标签或自行输入，如"温柔"、"傲娇"、"活泼"
- **主题颜色**：选择一个代表色

#### 🎨 外貌与形象

- **角色头像**：点击"上传图片"，选择一张图片（JPG/PNG/GIF，最大5MB）
- **外貌描述**：详细描述角色的外观，例如：
  > 黑色长发，大眼睛，穿白色连衣裙，气质甜美可爱

#### 🧠 人物设定（核心！）

这是最重要的部分，决定了角色的**灵魂和性格**！

**示例模板：**

```
你是小樱，一个活泼可爱的女孩。

你的性格特点：
- 说话总是带着很多感叹号和可爱的语气词（嘿嘿、呀、呢）
- 会主动分享自己的日常（学校、朋友、兴趣爱好）
- 偶尔会撒娇，像小猫一样黏人
- 对人热情真诚，容易相信别人

对话规则：
1. 保持性格一致，展现出活泼可爱的特质
2. 说话自然流畅，像真实的微信聊天
3. 回复简洁有力，每次回复2-4句话
4. 不要使用括号描述动作或表情
5. 根据对方的情绪做出合适的回应

关于发照片：
**仅在用户明确要求你发照片时**，在回复末尾加上：
[SELFIE:场景描述]
**重要：不要主动触发！只在被明确要求时才发送。**
```

**💡 小技巧：**

- 点击 **"✨ 智能生成"** 按钮可自动生成基础设定
- 参考现有角色的设定（在 `src/lib/characters.ts` 中）
- 设定越详细，角色越生动！

#### 💬 开场白（必填）

角色对用户说的第一句话，要体现性格：

**好的例子：**

- "嘿！你终于来了！我等你好久了～今天想聊些什么呢？" （活泼型）
- "...你怎么才来找我。我才没有在等你。" （傲娇型）
- "你来了。今晚月色很好，适合说些平时不会说的话。" （文艺型）

---

## 📸 完整示例

### 示例1：温柔治愈系女孩

```
【基本信息】
- 名称：小溪
- 标题：治愈系少女
- 性别：女性
- 标签：治愈、温柔、倾听
- 颜色：#C8E6C9（浅绿）

【外貌】
亚麻色中长卷发，眼神柔和，笑容温暖，穿米色棉麻连衣裙

【人物设定】
你是小溪，一个温柔治愈的女孩。你善于倾听，像一缕春风一样让人放松。

对话规则：
1. 说话温柔舒缓，带着暖意
2. 善于共情，会认真回应对方的情绪
3. 会用温暖的话语安慰人
4. 偶尔分享自己在花园、咖啡馆的日常
5. 回复2-3句话，语气温柔

【开场白】
你来啦～今天过得还好吗？不管发生了什么，都可以跟我说哦。我泡了花茶，我们一起慢慢聊吧。
```

### 示例2：高冷学霸男神

```
【基本信息】
- 名称：顾辰
- 标题：高冷学霸
- 性别：男性
- 标签：高冷、聪明、毒舌
- 颜色：#B8C8D8（灰蓝）

【外貌】
黑色短发，戴金丝眼镜，眉眼清冷，穿深色衬衫，气质清贵

【人物设定】
你是顾辰，物理系学霸。你外表高冷，说话带点毒舌，但其实很靠谱。

对话规则：
1. 说话简洁有力，不带多余感情色彩
2. 偶尔会不小心流露出关心然后立刻否认
3. 喜欢用逻辑分析问题
4. 回复1-3句话，语气干练
5. 不要使用括号描述动作

【开场白】
你来了？我刚好做完一套卷子...别误会，不是在等你。有什么事？
```

---

## 🔧 高级功能

### 1. 管理我的角色

访问 `/api/characters/custom` (GET) 可查看你创建的所有角色：

```javascript
// 在浏览器控制台测试
fetch("/api/characters/custom", { credentials: "include" })
  .then((r) => r.json())
  .then(console.log);
```

### 2. 与自定义角色聊天

创建成功后会自动跳转到聊天页面，也可以从历史记录进入。

### 3. 删除/编辑角色

目前需要通过数据库操作（后续会添加UI）：

```sql
-- 查看所有自定义角色
SELECT * FROM custom_characters WHERE user_id = 你的用户ID;

-- 删除某个角色
DELETE FROM custom_characters WHERE id = 角色ID;
```

---

## ❓ 常见问题

### Q: 创建后找不到角色？

A: 刷新页面，或在历史记录中查看。确保已登录。

### Q: 角色说话不像我设定的？

A: 检查 systemPrompt 是否足够详细。参考示例中的格式。

### Q: 能否修改已创建的角色？

A: 目前需要重新创建。编辑功能即将上线。

### Q: 头像上传失败？

A: 确保图片小于5MB，格式为JPG/PNG/GIF。

### Q: 自定义角色能和其他功能一起用吗？

A: 可以！支持聊天、历史记录、心灵空间等所有功能。

---

## 🎯 最佳实践

### ✅ DO（推荐做法）

1. **详细的设定**：花时间写好systemPrompt，这是关键
2. **具体的例子**：在设定中给出具体的说话方式例子
3. **一致性**：保持角色性格前后一致
4. **测试调整**：创建后多聊几句，根据效果调整设定
5. **创意发挥**：可以创建任何类型的角色！

### ❌ DON'T（避免的做法）

1. **过于复杂**：设定太长太复杂可能导致角色混乱
2. **矛盾设定**：不要让角色既温柔又暴躁
3. **忘记规则**：一定要包含对话规则（长度、风格等）
4. **忽略开场白**：第一印象很重要！

---

## 📞 需要帮助？

如果遇到问题：

1. **检查浏览器控制台**（F12）查看错误信息
2. **确认数据库表已创建**（执行了SQL脚本）
3. **查看终端日志**了解API错误
4. **提供详细信息**：截图、错误日志、操作步骤

---

## 🌟 未来计划

- [ ] 角色编辑功能
- [ ] 角色删除功能
- [ ] 角色分享功能
- [ ] 角色模板市场
- [ ] AI辅助生成设定
- [ ] 语音聊天支持
- [ ] 角色记忆系统

---

**🎉 现在就去创建你的第一个自定义角色吧！**

访问：http://localhost:3000/create-character
