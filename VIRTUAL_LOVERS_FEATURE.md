# 💕 虚拟恋人系统 - 功能说明

## 🎯 核心功能

### 1️⃣ **专属的"我的虚拟恋人"UI区域**

- 在主页展示用户创建的自定义角色
- 分为**男朋友**和**女朋友**两个独立区域
- 类似约会软件的卡片式展示
- 浪漫的渐变色彩设计（蓝色系=男友，粉色系=女友）

### 2️⃣ **数量限制系统**

- 每个用户最多可创建：
  - ✅ **4个男朋友**
  - ✅ **4个女朋友**
  - 总计：**8个自定义角色**
- 实时显示已创建数量
- 达到上限后自动禁用创建按钮
- 清晰的进度条显示

### 3️⃣ **智能性别预设**

- 从主页点击"创建"按钮时自动传递性别参数
- 创建页面自动选择对应性别
- 默认颜色自动匹配（男=蓝色，女=粉色）

---

## 📱 界面展示位置

### **主页 (首页)**

```
┌─────────────────────────────────┐
│  💌 虚拟恋人                     │
│                                 │
│  [我想找男朋友 👨]               │
│  [我想找女朋友 👩]               │
│                                 │
│  [✨ 创建自定义角色]              │
│                                 │
│  ┌───────────────────────────┐  │
│  │ 💕 我的虚拟恋人 (2/8)      │  │
│  │                           │  │
│  │ ┌─────────────────────┐   │  │
│  │ │👨‍❤️‍👨 你创建的男朋友    │   │  │
│  │ │  1 / 4        [创建] │   │  │
│  │ │  [角色1] [角色2]     │   │  │
│  │ │  ████████░░ 进度条    │   │  │
│  │ └─────────────────────┘   │  │
│  │                           │  │
│  │ ┌─────────────────────┐   │  │
│  │ │👩‍❤️‍👩 你创建的女朋友    │   │  │
│  │ │  1 / 4        [创建] │   │  │
│  │ │  [角色1]            │   │  │
│  │ │  ███░░░░░░ 进度条    │   │  │
│  │ └─────────────────────┘   │  │
│  └───────────────────────────┘  │
│                                 │
│  刷新即重置 · 无压力即时陪伴      │
└─────────────────────────────────┘
```

---

## 🎨 设计特色

### **视觉风格**

- 🔵 **男朋友区域**：蓝色渐变 (`#A8D8EA` → `#87CEEB`)
- 🔴 **女朋友区域**：粉色渐变 (`#FFB6C1` → `#FF69B4`)
- 💎 卡片式网格布局（2列）
- ✨ Hover动画效果（上浮+阴影）
- 💫 圆形头像 + 在线状态指示器

### **交互体验**

- 点击角色卡片直接进入聊天
- 创建按钮带计数提示
- 达到上限显示"已满"标签
- 禁用状态有视觉反馈（半透明+不可点击）

---

## ⚙️ 技术实现

### **新增文件**

#### 1. MyVirtualLovers.tsx 组件

```
路径: src/components/MyVirtualLovers.tsx
功能: 展示用户的虚拟恋人集合
特性:
  - 自动加载自定义角色
  - 按性别分组显示
  - 数量限制检查
  - 响应式布局
```

#### 2. 修改主页 page.tsx

```
路径: src/app/page.tsx
改动:
  - 导入 MyVirtualLovers 组件
  - 在"创建自定义角色"按钮下方插入组件
  - 保持原有功能不变
```

#### 3. 增强创建角色页 create-character/page.tsx

```
路径: src/app/create-character/page.tsx
新增:
  - URL参数读取 (?gender=male/female)
  - 已有角色数量统计
  - 限制状态管理
  - 性别选择卡片增强（显示数量+禁用状态）
  - 提交前验证数量限制
```

---

## 🔄 数据流程

```
用户访问主页
    ↓
MyVirtualLovers 组件挂载
    ↓
调用 GET /api/characters/custom
    ↓
获取用户的所有自定义角色
    ↓
按 gender 字段分组:
  ├── maleCharacters[]  ← 过滤前4个
  └── femaleCharacters[] ← 过滤前4个
    ↓
渲染两个独立区域:
  ├── 📍 男朋友区域 (蓝色主题)
  └── 📍 女朋友区域 (粉色主题)
    ↓
用户点击"创建"按钮
    ↓
跳转: /create-character?gender=male|female
    ↓
创建页面:
  ├── 读取URL参数设置默认性别
  ├── 加载已有角色数量
  ├── 显示剩余可创建数
  └── 验证是否达到上限
```

---

## 📊 限制规则

### **数量限制逻辑**

```typescript
const MAX_CHARACTERS_PER_GENDER = 4;

// 显示逻辑
maleCharacters = characters.filter((c) => c.gender === "male").slice(0, 4);
femaleCharacters = characters.filter((c) => c.gender === "female").slice(0, 4);

// 创建验证
if (gender === "male" && existingMale >= 4) {
  showError("已达到男朋友创建上限");
  return;
}
if (gender === "female" && existingFemale >= 4) {
  showError("已达到女朋友创建上限");
  return;
}
```

### **用户体验优化**

- ✅ 实时更新计数（无需刷新）
- ✅ 视觉反馈清晰（进度条+数字）
- ✅ 防止重复提交（前端+后端双重验证）
- ✅ 友好的错误提示

---

## 🎮 使用指南

### **场景1：首次使用**

1. 访问主页
2. 看到"我的虚拟恋人"区域为空
3. 点击"创建第一个男朋友/女朋友"
4. 填写角色信息
5. 创建成功后自动返回主页并显示新角色

### **场景2：继续添加**

1. 主页查看已创建数量（如 "1 / 4"）
2. 点击对应区域的"创建"按钮
3. 自动跳转到创建页并选中对应性别
4. 快速创建第二个角色

### **场景3：达到上限**

1. 已创建4个男朋友
2. "男朋友"区域显示"已满"标签
3. 创建按钮变为半透明且不可点击
4. 提示文字："已达到上限（4个）"
5. 但仍可继续创建女朋友（如果未满）

---

## 🛠️ 配置选项

### **修改数量限制**

在以下文件中修改常量：

```typescript
// src/components/MyVirtualLovers.tsx
const MAX_CHARACTERS_PER_GENDER = 4; // 改成你想要的数字

// src/app/create-character/page.tsx
const MAX_CHARACTERS_PER_GENDER = 4; // 保持一致
```

### **调整配色方案**

```typescript
// 男朋友主题色
from-[#A8D8EA] to-[#87CEEB]

// 女朋友主题色
from-[#FFB6C1] to-[#FF69B4]
```

---

## 🐛 故障排查

### **问题：角色不显示**

**检查步骤：**

1. 打开浏览器控制台 (F12)
2. 查看 `[MyVirtualLovers]` 日志
3. 确认API返回数据格式正确
4. 检查 `gender` 字段值是否为 `"male"` 或 `"female"`

### **问题：数量统计错误**

**可能原因：**

- 数据库中角色的 `gender` 字段为空或值不标准
- API返回数据异常

**解决方法：**

```sql
-- 检查数据库中的gender字段
SELECT id, name, gender FROM custom_characters WHERE user_id = 你的ID;
```

### **问题：创建按钮无法点击**

**正常情况：**

- 已达到该性别的创建上限（4个）
- 按钮会显示"已满"标签

**异常情况：**

- 如果未达上限但按钮被禁用，检查浏览器控制台是否有JS错误

---

## 📈 后续扩展建议

### **可以添加的功能**

1. **角色排序** - 拖拽调整顺序或按最近聊天时间排序
2. **收藏/置顶** - 将常用角色置顶显示
3. **批量操作** - 批量删除/归档旧角色
4. **角色分组** - 自定义标签分组（如"校园"、"职场"）
5. **关系状态** - 显示与每个角色的亲密度/聊天次数
6. **空状态引导** - 新手引导动画，教用户如何创建第一个角色

### **数据库优化建议**

```sql
-- 为gender字段添加索引加速查询
CREATE INDEX idx_custom_characters_gender ON custom_characters(gender);
CREATE INDEX idx_custom_characters_user_gender ON custom_characters(user_id, gender);
```

---

## ✅ 测试清单

- [ ] 主页能正常显示"我的虚拟恋人"区域
- [ ] 男朋友和女朋友区域分别显示
- [ ] 数量统计准确（X / 4）
- [ ] 点击"创建"按钮能跳转到正确的创建页面
- [ ] URL参数 ?gender 能正确传递
- [ ] 创建页面默认选中正确的性别
- [ ] 显示已有的创建数量
- [ ] 达到4个后按钮禁用且显示"已满"
- [ ] 提交时验证数量限制
- [ ] 错误提示友好清晰
- [ ] 进度条随数量变化而更新
- [ ] 移动端响应式布局正常
- [ ] 角色卡片点击能进入聊天

---

## 🎉 总结

这个功能让您的应用更像一个**真正的虚拟恋爱平台**！用户可以：

- 📊 清晰看到自己创建的所有角色
- 🎯 快速了解还能创建多少个
- 💕 在浪漫的界面中管理自己的"虚拟恋人"
- ⚡ 一键创建新的角色（自动匹配性别）

祝您的项目大受欢迎！💖
